// Placeholder for a gift icon. Replace with your own image if needed.
import React from 'react';

const GiftIcon = ({ style }) => (
  <span role="img" aria-label="Gift" style={style}>🎁</span>
);

export default GiftIcon;
